import { Connection } from 'mongoose';
import { DatabaseHealthIndicator } from './database-health-indicator';
export declare class MongooseHealthIndicator extends DatabaseHealthIndicator {
    readonly connection: Connection;
    /**
     * Initializes the typeorm indicator
     *
     * @param connection The typeorm connection of the application context
     *
     * @public
     */
    constructor(connection: Connection);
    /**
     * Pings a mongoose connection
     * @param connection The connection which the ping should get executed
     * @param timeout The timeout how long the ping should maximum take
     *
     */
    pingDb(connection: Connection, timeout: number): Promise<unknown>;
}
